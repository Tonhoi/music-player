export const initialState = {
    searchValue: '',
    searchResult: [],
    debounce: false,
};
const rootReducer = (state = initialState, action) => {
    switch (action.type) {
        case '':
            break;

        default:
            break;
    }
};

export default rootReducer;
